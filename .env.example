# Ruta al fichero de configuración compartida (JSON plano)
# Si no se define, se usa la carpeta userData de Electron
# SHARED_CONFIG_PATH=\\SERVIDOR\AppControlBackup\config-shared.json

# URL del servidor Vite en desarrollo (solo para dev)
# VITE_DEV_SERVER_URL=http://localhost:5174/
