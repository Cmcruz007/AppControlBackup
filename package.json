{
  "name": "backup-monitor",
  "version": "1.0.0",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:win": "npm run build && electron-builder --win dir"
  },
  "dependencies": {
    "mssql": "^10.0.0"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.0.0",
    "vite": "^5.0.0"
  },
  "build": {
    "appId": "com.uci.backupmonitor",
    "productName": "BackupMonitor",
    "directories": {
      "output": "release"
    },
    "win": {
      "target": [
        "dir"
      ]
    },
    "files": [
      "dist/**/*",
      "main.cjs",
      "preload.cjs"
    ]
  }
}
